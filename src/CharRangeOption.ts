export interface CharRangeOptions {
  control?: boolean,
  tab?: boolean,
  linebreak?: boolean,
  space?: boolean,
  punct?: boolean,
  alnum?: boolean,
  privateUse?: boolean,
  supplementary?: boolean,
}
